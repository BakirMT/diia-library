rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all reads/writes for this example. For production, secure this!
      allow read, write: if true;
    }
  }
}
