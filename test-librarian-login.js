import { initializeApp } from "firebase/app";
import { getFirestore, collection, addDoc, getDocs, collectionGroup } from "firebase/firestore";

// Cannot run firebase directly without config, but I can check logic
console.log("Syntax is correct");
